# LLM Proxy 项目任务书

> 生成日期：2026年4月7日
> 项目路径：`G:\Desktop\Code\API-Redirect\llm-proxy`

---

## 一、准备阶段

| 序号 | 任务 | 说明 |
|------|------|------|
| 1 | 准备 MySQL 数据库 | 创建 `llm_proxy` 数据库 |
| 2 | 准备 Redis 服务 | 确保 Redis 可用 |
| 3 | 配置 `config.yaml` | 修改数据库、Redis、邮件等配置 |

---

## 二、核心业务逻辑开发

### 阶段1：数据访问层 & 工具层 ✅

| 序号 | 文件 | 函数/功能 | 说明 |
|------|------|----------|------|
| 1 | `repositories/user_repo.py` | `get_by_email()` | 需要 db 参数 |
| 2 | `repositories/user_repo.py` | `get_by_username()` | 需要 db 参数 |

### 阶段2：Redis 会话管理 ✅

| 序号 | 文件 | 函数/功能 | 说明 |
|------|------|----------|------|
| 3 | `utils/redis_client.py` | **新建** - Redis 客户端封装 | 提供 `set/get/delete` 方法，用于存储 Session、验证码等 |

### 阶段3：认证服务 (`services/auth_service.py`) ✅

| 序号 | 行号 | 函数 | 具体任务 |
|------|------|------|----------|
| 4 | 53 | `send_register_code()` | 将生成的验证码存入 Redis |
| 5 | 73 | `register()` | 从 Redis 获取并验证验证码 |
| 6 | 105 | `register()` | 将 Session 存入 Redis |
| 7 | 127 | `login()` | 将 Session 存入 Redis |
| 8 | 139 | `logout()` | 从 Redis 删除 Session |
| 9 | 158 | `delete_account()` | 验证验证码（从 Redis） |
| 10 | 167 | `delete_account()` | 删除 Redis 中的所有用户 Session |
| 11 | 173 | `validate_session()` | 从 Redis 获取 Session，对应用户信息，并续期 |
| 12 | 181 | `refresh_session()` | 刷新 Redis 中的 Session TTL |

### 阶段4：用户服务 (`services/user_service.py`) ✅

| 序号 | 行号 | 函数 | 具体任务 |
|------|------|------|----------|
| 13 | 33 | `get_profile()` | 实现获取用户信息逻辑（余额、月消费、API-Key列表等） |
| 14 | 40 | `get_bills()` | 实现获取账单逻辑 |
| 15 | 47 | `get_daily_stats()` | 实现获取每日统计数据逻辑 |
| 16 | 54 | `deduct_balance()` | 实现扣除余额（原子操作） |
| 17 | 61 | `add_balance()` | 实现增加余额 |

### 阶段5：计费服务 (`services/billing_service.py`) ✅

| 序号 | 行号 | 函数 | 具体任务 |
|------|------|------|----------|
| 18 | 63 | `record_usage()` | 调用 `UserService.deduct_balance()` 扣除余额 |

### 阶段6：代理转发服务 (`services/proxy_service.py`)

| 序号 | 行号 | 函数 | 具体任务 |
|------|------|------|----------|
| 19 | 36 | `get_upstream_config()` | 从数据库获取上游配置（支持动态配置） |
| 20 | 90 | `proxy_chat_completions()` | `api_key_id=0` 改为从认证中获取真实的 api_key_id |
| 21 | 113 | `proxy_claude_messages()` | 实现 Claude 消息转发逻辑 |
| 22 | 123 | `proxy_gemini()` | 实现 Gemini 转发逻辑 |
| 23 | 133 | `proxy_openai_responses()` | 实现 OpenAI Responses 转发逻辑 |

### 阶段7：充值服务 (`services/recharge_service.py`)

| 序号 | 行号 | 函数 | 具体任务 |
|------|------|------|----------|
| 24 | 59 | `handle_paypal_callback()` | 验证 PayPal 回调签名 |
| 25 | 60 | `handle_paypal_callback()` | 更新订单状态 |
| 26 | 61 | `handle_paypal_callback()` | 增加用户余额 |

### 阶段8：Session 中间件 (`middleware/session.py`)

| 序号 | 行号 | 函数 | 具体任务 |
|------|------|------|----------|
| 27 | 23 | `dispatch()` | 验证 session 有效性并自动续期 |

### 阶段9：API 路由层 - 认证接口 (`api/v1/auth.py`) ✅

| 序号 | 行号 | 问题 | 说明 |
|------|------|------|------|
| 28 | 19 | `auth_service.register()` | 缺少 `db` 参数，需从 `Depends(get_db)` 获取 |

### 阶段10：API 路由层 - 用户接口 (`api/v1/user.py`) ✅

| 序号 | 行号 | 问题 | 说明 |
|------|------|------|------|
| 29 | 16 | `user_service` | 需要注入 db Session |

### 阶段11：API 路由层 - API-Key接口 (`api/v1/apikey.py`) ✅

| 序号 | 行号 | 问题 | 说明 |
|------|------|------|------|
| 30 | 全部 | `apikey_service` | 需要注入 db Session |

### 阶段12：API 路由层 - 充值接口 (`api/v1/recharge.py`) ✅

| 序号 | 行号 | 问题 | 说明 |
|------|------|------|------|
| 31 | 全部 | `recharge_service` | 需要注入 db Session |

---

## 三、数据库初始化

| 序号 | 文件 | 任务 |
|------|------|------|
| 32 | `scripts/init_db.py` | 运行初始化，创建所有表 |

---

## 四、测试开发（可选，后期）

| 序号 | 文件 | 函数 |
|------|------|------|
| 33 | `tests/test_auth.py` | 7 个测试函数 |
| 34 | `tests/test_apikey.py` | 5 个测试函数 |
| 35 | `tests/test_proxy.py` | 5 个测试函数 |

---

## 五、部署

| 序号 | 任务 | 说明 |
|------|------|------|
| 36 | 服务器准备 | 安装 Python 3.10+, MySQL, Redis |
| 37 | 安装依赖 | `pip install -r requirements.txt` |
| 38 | 初始化数据库 | `python scripts/init_db.py` |
| 39 | 启动服务 | `uvicorn app.main:app --host 0.0.0.0 --port 8000` |
| 40 | 配置反向代理 | Nginx/Caddy + HTTPS |
| 41 | 配置系统服务 | systemd/supervisor 自动启动 |

---

## 开发优先级建议

```
优先级 1（核心）: 阶段2 → 阶段3 → 阶段4 → 阶段5 → 阶段6
优先级 2（接口）: 阶段7 → 阶段8 → 阶段9-12
优先级 3（完善）: 阶段1（修复repo问题）→ 阶段3
优先级 4（测试/部署）: 阶段4 → 阶段5
```

---

## 任务统计

| 类别 | 数量 |
|------|------|
| 核心业务逻辑 | 26 项 |
| 测试开发 | 3 项 |
| 部署任务 | 6 项 |
| **总计** | **35 项** |
