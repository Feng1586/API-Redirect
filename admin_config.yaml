# ============================================================
# 模型管理工具 - 数据库配置文件
# ============================================================
# 使用说明：
# 1. 根据实际环境修改以下数据库连接信息
# 2. 确保运行脚本的主机能访问该数据库
# 3. 需要安装依赖：pip install sqlalchemy pymysql pyyaml
# ============================================================

database:
  # 数据库主机地址
  host: "192.168.2.101"

  # 数据库端口（MySQL 默认 3306）
  port: 3306

  # 数据库用户名
  username: "llm_proxy"

  # 数据库密码
  password: "NaiCbtEDxtZ428Fw"

  # 数据库名称
  name: "llm_proxy"
